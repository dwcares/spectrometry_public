# Wind Tunnel - 2-D fluid dynamics (lattice-Boltzmann + compressible Navier-Stokes) video engine.
# ffmpeg is an external program: put it on PATH, set WT_FFMPEG, or drop ffmpeg.exe into src/bin/.
numpy>=2.0
Pillow>=10.0
scipy>=1.11

# Optional, NVIDIA GPUs only (Windows / Linux). Much faster; without it everything runs on the
# CPU automatically. The [ctk] extra bundles its own CUDA 12 pieces, so no system CUDA install:
#   pip install "cupy-cuda12x[ctk]>=13.0"
