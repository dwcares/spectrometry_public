[CLI]
# Output lands in ./renders (flat, no quality subfolders) next to this file.
media_dir = ./renders
flush_cache = False
frame_rate = 30
# Dark background, matching the rest of the channel
background_color = #0a0d12
# Don't auto-open the player after each render
preview = False
